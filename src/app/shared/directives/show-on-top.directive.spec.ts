import { ShowOnTopDirective } from './show-on-top.directive';

describe('ShowOnTopDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowOnTopDirective();
    expect(directive).toBeTruthy();
  });
});
